defmodule GravitonMQ do
  @moduledoc """
  Public namespace for the embeddable GravitonMQ application.

  Milestone 0 intentionally exposes no broker operations. There is no working
  AMQP listener, publish path, consumer path, queue, or persistence engine.
  """
end
