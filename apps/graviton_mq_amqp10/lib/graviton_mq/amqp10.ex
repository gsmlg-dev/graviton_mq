defmodule GravitonMQ.AMQP10 do
  @moduledoc """
  Owns AMQP 1.0 protocol concepts and their mapping boundary to the broker core.

  Milestone 0 provides data and type boundaries only. It does not provide a
  codec, transport, negotiation, listener, or compatibility claim.
  """
end
